#!/bin/sh

cd /tmp/src

if [ -d /tmp/artifacts ] && [ "$(ls -A /tmp/artifacts)" ]; then
    mv /tmp/artifacts/* .
fi

if [ ! -f content.lnk ] && [ ! -f content.txt ]; then
    echo 'Content shown from symbolic link' > content.txt
    ln -s content.text content.lnk
fi
